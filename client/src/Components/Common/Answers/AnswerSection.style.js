import styled from "styled-components"

export const MultiAnswer = styled.div`
  display: flex;
  flex-wrap: wrap;

  h4 {
    width: 100%;
  }

  div {
    background: var(--primary);
    border: 2px dotted white;
    border-radius: 3px;
    color: white;
    padding: 0.5rem;
    margin-right: 0.25rem;
    margin-bottom: 0.25rem;
  }
`
